import Validator from 'dummy/utils/validator';
import { module, test } from 'qunit';

module('Unit | Utility | validator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = new Validator();
  assert.ok(result);
});

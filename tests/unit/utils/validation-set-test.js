import validationSet from 'dummy/utils/validation-set';
import { module, test } from 'qunit';

module('Unit | Utility | validation set');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validationSet();
  assert.ok(result);
});

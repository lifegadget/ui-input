import { typeOf } from '../../../helpers/type-of';
import { module, test } from 'qunit';

module('Unit | Helper | type of');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = typeOf([42]);
  assert.ok(result);
});

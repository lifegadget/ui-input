<div class="input-group-btn {{name}}">
  {{#if hasBlock}}
    {{#ui-button title=title icon=icon mood=mood}}
      {{yield}}
    {{/ui-button}}
  {{else}}
    {{ui-button title=title icon=icon mood=mood}}
  {{/if}}
</div>
